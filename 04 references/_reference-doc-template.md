# [reference name]

<!-- One md per reference, named [reference name].md, asset in assets/. A reference is external inspiration the user gathered — a layout, a texture, a color scheme, a typography treatment, an existing logo. Two things carry: the DESCRIPTION (what it is, objectively) and the user's COMMENTARY (their read — what they like, what they'd change, what to take from it). The commentary is the load-bearing part: it's what the expression-doc-writer turns into locked controls. The asset is what the agent studies to fill the visual detail. This is the DRAPER app's export shape, merged into one file per reference. -->

## Description

[What the reference is, objectively — the layout, texture, type treatment, color, or mark. AI-generated is fine. This is what the agent looks at the asset to confirm.]

## User context

[The user's raw commentary on this reference, verbatim. What they like, what they'd change, what specifically to take from it, what to ignore. This is the source of locked controls — keep it exact, hedges and all. If the reference is an existing brand element (a logo), say so here.]

## Assets

- [[reference filename]](./assets/[reference filename])

## Asset CDN Links

- [[reference filename]]([CDN URL])
